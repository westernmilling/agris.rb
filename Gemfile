# frozen_string_literal: true
source 'https://rubygems.org'

# Specify your gem's dependencies in agris.gemspec
gemspec

# rubocop 0.54 calls the positional Psych.safe_load signature, removed in
# Psych 4 (bundled from Ruby 3.1). Pin until rubocop is modernised.
gem 'psych', '~> 3.3'
